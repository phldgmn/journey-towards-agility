# journey-towards-agility

To install, use
`pip install -r requirements.txt`
and
`pip install -r tmtoolkit/requirements_py27.txt`

To evaluate the LDA model, go to
`tmtoolkit/00-lda-evaluation`
and execute
`python lda_evaluation.py`
You might need to change `initialize_data` in this file.
